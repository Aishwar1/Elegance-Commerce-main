## Packages
framer-motion | Page transitions and scroll-triggered animations
zustand | Simple, fast global state management for the shopping cart
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility to merge tailwind classes without style conflicts

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["Geist", "sans-serif"],
  display: ["Space Grotesk", "sans-serif"],
  mono: ["Space Mono", "monospace"],
}

Images:
Logo is imported statically from @assets/image_1772438336955.png
Products use Unsplash placeholders if missing, but schema provides imageUrl.
